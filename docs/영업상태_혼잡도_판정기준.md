# 영업상태(final_status)·혼잡도(congestion_level) 판정 기준

> 이 문서는 손으로 쓴 레퍼런스입니다(자동 생성 아님 — 코드를 고치면 이 문서도 같이 갱신할 것).
> 대상 테이블: `store_operating_status`(`db/sql/schema.sql` 5번). 코드 위치는 각 절 끝의 "코드 위치" 참고.
> 수치는 전부 2026-09-03 기준 실측 DB 값입니다(21개 매장, `store_operating_status` 267,582행).

---

## 0. 요약

| 필드 | 테이블.컬럼 | 값 | 경계 방식 |
|---|---|---|---|
| `final_status` | `store_operating_status.final_status` | `영업중`\|`휴무추정`\|`예외영업`\|`영업종료` | `schedule_status`×`power_status` 2×2 매트릭스(고정 규칙, 매장마다 같음) |
| `congestion_level` | `store_operating_status.congestion_level` | `상`\|`중`\|`하`\|`NULL` | **매장(계기)별** 이용률 분위수(Q1/Q3) — 매장마다 다른 절대값 |

---

## 1. `final_status` — 4개 값의 판정 기준

### 1-1. 입력이 되는 두 하위 판정

**`schedule_status`** (`open_hours` \| `closed_hours`)
- 소스: `store_operating_hours`(`source='google_places'` 실측 우선, 없으면 `'ksic_estimate'` 추정)
- 코드: `db/src/ami_db/status.py`의 `determine_schedule_status()`(스칼라) / `db/scripts/09_compute_operating_status.py`의 `compute_status_for_store()`(벡터화 배치)
- 규칙: 조회 시각이 그 요일의 `[open_time, close_time]` 안이면 `open_hours`. `is_24h=true`면 항상 `open_hours`. `is_closed=true`이거나 시간 정보 자체가 없으면 `closed_hours`(정보 없음은 안전하게 닫힘 취급). `close_time < open_time`이면 자정을 넘기는 영업(예: 17:00\~익일 04:00)으로 간주해 "open 이후이거나 close 이전"으로 판정.

**`power_status`** (`active` \| `low`)
- 소스: `meter_timeseries.received_active_power_kwh`(해당 슬롯의 실측 또는 코호트 재분배값)
- 코드: `db/src/ami_db/status.py`의 `determine_power_status()` / 09단계 벡터화 버전
- 공식:
  ```
  night_baseline = median(그 계기의 0\~5시 recv_kWh, 실측 91일치)
  floor = max(night_baseline, 0.01)
  power_status = "active"  if recv_kWh >= floor × 1.5
               = "low"     if recv_kWh <  floor × 1.5   또는 recv_kWh가 NULL
  ```
  `1.5`(`ACTIVE_RATIO_THRESHOLD`)는 야간 대비 몇 배부터 "가동 중"으로 볼지의 배수. NULL을 무조건 `low`로 방어 처리하는 이유: 결측을 `active`로 잘못 판단하는 것보다 안전한 쪽으로 치우치기 위함.
- `data_resolution='1hour'`인 4개 매장(A-L-16/19/49/70)은 이 결측이 "가끔"이 아니라 슬롯의 75%가 상시·구조적으로 없으므로, 이 low-방어 규칙 자체를 적용하지 않고 그 슬롯의 판정을 아예 만들지 않는다(09단계, `data_resolution` 분기 — `db/docs/SCHEMA.md`의 "data_resolution/is_redistributed" 절 참고).

### 1-2. 2×2 매트릭스 (코드: `status.FINAL_STATUS_MATRIX`)

| `schedule_status` \ `power_status` | `active` | `low` |
|---|---|---|
| `open_hours` | **영업중** | **휴무추정** |
| `closed_hours` | **예외영업** | **영업종료** |

### 1-3. 각 값의 실측 빈도 (267,582행 기준)

| 값 | 행 수 | 비율 | 의미 |
|---|---:|---:|---|
| 영업종료 | 89,869 | 33.59% | 영업시간표 밖 + 전력 낮음 — 가장 흔한 정상 케이스(심야 등) |
| 영업중 | 81,726 | 30.54% | 영업시간표 안 + 전력 정상 |
| 휴무추정 | 57,476 | 21.48% | 영업시간표상 열려있어야 하는데 전력이 야간 수준 |
| 예외영업 | 38,511 | 14.39% | 영업시간표상 닫혀있어야 하는데 전력이 야간 대비 1.5배 이상 |

### 1-4. "휴무추정" vs "예외영업" — 무엇이 다른가

| | 휴무추정 | 예외영업 |
|---|---|---|
| `schedule_status` | `open_hours`(표상 열려있어야 함) | `closed_hours`(표상 닫혀있어야 함) |
| `power_status` | `low`(전력 낮음) | `active`(전력 높음) |
| 어긋나는 방향 | 열려있어야 하는데 조용함 | 닫혀있어야 하는데 활발함 |
| 원문 근거 | `data/프로젝트개요.md` 원문이 명시한 유일한 케이스("영업중 전력량이 영업종료 시간과 같을시 영업 종료로 판단") | 원문에 명시적 언급 없음 — 코드 주석 "심야영업 등(관리자 알림 후보)"이 유일 근거 |

**휴무추정은 원문이 요구한 핵심 기능이라 설계 의도가 명확**하지만, **예외영업은 원문에 없는, 구현 과정에서 매트릭스의 나머지 한 칸을 채우려고 추가된 값**이라는 점이 근본적으로 다르다.

### 1-5. "예외영업"의 리스크 — 실측으로 확인된 문제

**빈도 자체가 "가끔"이 아니다.** 전체 슬롯의 14.39%(38,511행)로, `영업중`(30.54%)의 절반 가까이 된다. "심야영업 등 드문 예외"라는 이름의 함의와 실제 발생 빈도가 맞지 않는다.

**시간대별로 쪼개보면 "심야영업"이 지배적 원인이 아니다:**

| 시간대 | 예외영업 건수 | 비율 |
|---|---:|---:|
| 07\~10시 | 20,953 | 54.4% |
| 22\~23시(원래 의도한 "심야영업") | 3,460 | 9.0% |
| 나머지(0\~6, 11\~21시) | 14,098 | 36.6% |

**54.4%가 07\~10시에 몰려있다.** 이 시간대는 대부분 매장의 영업시작 추정시각 이전이다 — 즉 "심야영업"이 아니라 **영업 준비시간(재료 손질·예열·청소 등으로 야간보다 전력은 높지만 아직 개점 전인 상태)**이 지배적 원인으로 추정된다. 원래 이 값이 잡으려던 "22\~23시대 심야영업" 케이스는 오히려 9.0%로 소수다.

**운영시간 정보의 정밀도(구글 실측 vs KSIC 추정) 문제가 아니다** — 예외영업 최다 발생 매장(마티니 7,383건, 25센치꼬치앤오뎅바 3,323건 등) 상당수가 `google_places` 실측 운영시간을 쓰는 매장이다. 즉 "운영시간을 더 정확히 알면 해결된다"가 아니라, **`active_ratio_threshold=1.5`가 아침 준비시간대의 전력 상승을 걸러내지 못하는 임계값 설계 자체의 한계**로 판단된다.

**결론 — 이 값을 그대로 관리자 알림(2순위, 아직 미구현)의 트리거로 쓰면 안 된다.** 지금 코드에 이 완화 로직은 없다. `serving_api.py`의 `/api/anomalies`가 "영업종료 이후"만 필터링해 안전감지(2순위)는 이미 부분적으로 회피하고 있지만, `final_status='예외영업'` 라벨 자체는 여전히 넓게(전체의 14%) 잡히므로 이 필드를 그대로 사람에게 보여주거나 알림에 쓰면 아침 준비시간을 심야영업으로 오인시킬 위험이 크다.

**코드 위치**: `db/src/ami_db/status.py`(`determine_schedule_status`/`determine_power_status`/`FINAL_STATUS_MATRIX`), `db/scripts/09_compute_operating_status.py`(`compute_status_for_store`).

---

## 2. `congestion_level` — 3개 값의 판정 기준

### 2-1. 공식

```
이용률(%) = received_active_power_kwh × 4 ÷ contract_power_kw × 100
```
(×4는 15분 kWh를 1시간 기준 순간 kW로 환산하는 계수)

### 2-2. 경계 — 매장별 개인화 근거

**전역 고정 % 컷이 아니라 계기(매장)마다 자기 자신의 이용률 분위수(Q1/Q3)를 상/중/하 경계로 쓴다:**

```
congestion_level = "하"   if util ≤ Q1
                  = "중"   if Q1 < util ≤ Q3
                  = "상"   if util > Q3
                  = NULL   if final_status ≠ "영업중"   (영업중이 아니면 혼잡도 자체가 무의미)
```

Q1/Q3는 그 매장의 전체 이용률 표본(`meter_timeseries` 전체 — 실측+합성, NULL 제외)에서 매장마다 각자 계산한다. 전역에서 공유하는 단일 컷이 아니다.

**코드 위치**: `db/src/ami_db/status.py`의 `compute_utilization_quartiles()` / `determine_congestion_level()`, `db/scripts/09_compute_operating_status.py`.

### 2-3. 통계적 근거 — 왜 전역 컷이 아니라 매장별 분위수인가

1. **비정규 분포 확인됨** — `visualize_analyis_data/docs/05_시각분석_AI방법론_보고서.md` 4-1절: 대표 4개 계기 전부 정규성 기각(왜도 0.72\~2.45, 첨도 −0.46\~7.97). 평균±표준편차 기반 컷은 이 데이터에 적용할 수 없다.
2. **계약전력 스케일이 매장마다 15배 차이** — 21개 매장의 `contract_power_kw`가 3kW\~45kW로 분포(아래 2-4절 표). 절대 kWh로 컷을 두면 대형 계약전력 매장은 항상 "하", 소형 매장은 항상 "상"으로 쏠린다 → 이용률(%)로 정규화해도 매장 간 "정상적인 바쁨의 크기" 자체가 다르므로 절대 %컷도 부적절.
3. **소규모·저압 매장의 분산이 특히 큼** — `05_...` 4-4절 실측 근거. 이 때문에 매장 자신의 분포 안에서 상대적 위치(분위수)로 판정하는 것이, 그 매장 입장에서 "평소보다 바쁜가"를 가장 직접적으로 반영한다.

### 2-4. 실제 21개 매장 현재 경계값 (2026-09-03, DB 실측)

| store_id | 상호 | meter_id | 계약전력(kW) | 해상도 | Q1(하/중 경계) | Q3(중/상 경계) | IQR | 표본수 |
|---:|---|---|---:|---|---:|---:|---:|---:|
| 1 | 못난이찹쌀꽈배기 | A-L-11 | 8 | 15min | 9.60% | 44.10% | 34.50%p | 14,877 |
| 2 | 플랜트스터디카페까치산역점 | A-L-16 | 4 | 1hour | 12.40% | 25.14% | 12.74%p | 3,723 |
| 3 | 성균관한자교습소 | A-L-19 | 4 | 1hour | 14.20% | 39.96% | 25.76%p | 3,720 |
| 4 | 워시엔조이까치산역점 | A-L-27 | 5 | 15min | 7.20% | 37.92% | 30.72%p | 14,877 |
| 5 | 코다 | A-L-30 | 15 | 15min | 9.76% | 28.24% | 18.48%p | 14,876 |
| 6 | 마티니 | A-L-34 | 25 | 15min | 14.98% | 45.30% | 30.32%p | 14,877 |
| 7 | 훈이네주스 | A-L-37 | 3 | 15min | 0.75% | 0.93% | **0.19%p** | 14,876 |
| 8 | 영연 | A-L-39 | 15 | 15min | 14.91% | 25.13% | 10.23%p | 14,877 |
| 9 | 이리스디자인 | A-L-49 | 3 | 1hour | 11.07% | 26.00% | 14.93%p | 3,720 |
| 10 | 한가족반찬 | A-L-57 | 10 | 15min | 14.36% | 31.52% | 17.16%p | 14,878 |
| 11 | 조박사소머리국밥 | A-L-58 | 5 | 1hour(재분배) | 67.30% | 190.63% | 123.33%p | 14,880 |
| 12 | 꽃게왕야스다가(家) | A-L-60 | 45 | 15min | 8.45% | 21.88% | 13.43%p | 14,878 |
| 13 | 디어마이 | A-L-61 | 10 | 15min | 11.00% | 22.00% | 11.00%p | 14,878 |
| 14 | 통툰만화카페까치산역점 | A-L-62 | 15 | 15min | 17.79% | 36.75% | 18.96%p | 14,619 |
| 15 | 곰달면옥 | A-L-63 | 13 | 15min | 8.15% | 52.55% | 44.40%p | 14,878 |
| 16 | 25센치꼬치앤오뎅바 | A-L-65 | 45 | 15min | 8.09% | 25.92% | 17.83%p | 14,878 |
| 17 | 꽃드림 | A-L-70 | 4 | 1hour | 15.76% | 34.55% | 18.79%p | 3,720 |
| 18 | 정들옛날치킨불닭발 | A-L-71 | 12 | 15min | 8.27% | 16.02% | 7.75%p | 14,878 |
| 19 | 대박해물찜 | A-L-72 | 25 | 15min | 4.32% | 10.35% | 6.03%p | 14,878 |
| 20 | 요달의찜닭&내가닭도리탕 | A-L-79 | 20 | 15min | 3.46% | 7.34% | 3.88%p | 14,878 |
| 21 | 두찜강서까치산역점 | A-L-80 | 19 | 15min | 4.57% | 31.58% | 27.01%p | 14,878 |

재계산 방법(재현용): `received_active_power_kwh × 4 ÷ contract_power_kw × 100`의 `percentile_cont(0.25)`/`percentile_cont(0.75)` — `meter_timeseries`와 `meters`를 `meter_id`로 조인, `received_active_power_kwh IS NOT NULL`만 대상.

### 2-5. 알려진 통계적 약점 — 퇴화(degenerate) 케이스

`IQR = Q3 − Q1`이 아주 작으면 Q1≈Q3라 "중" 구간이 사실상 존재하지 않아, 아주 작은 전력 변동에도 "상"↔"하"가 튄다(`visualize_analyis_data/docs/06_혼잡도_안전감지_방법론_보고서.md` 1-1절이 `IQR < 3%p`를 폴백 기준으로 제안).

이 기준(3%p)을 21개 매장에 적용하면 **`A-L-37`(훈이네주스, store_id=7) 단 하나만 해당** — `Q1=0.75%`, `Q3=0.93%`, `IQR=0.19%p`로 극단적으로 좁다(계약전력 3kW의 소형 매장, 이용률 자체가 상시 낮고 평평함). 다음으로 좁은 `A-L-79`(요달의찜닭, IQR=3.88%p)는 3%p 기준을 넘어 퇴화로 분류되지 않는다.

**⚠ 현재 실행 중인 `09_compute_operating_status.py`에는 이 퇴화 케이스에 대한 폴백이 구현돼 있지 않다.** 위 보고서의 "고정 배율 밴드(`[median×0.7, median×1.3]`)로 폴백" 제안은 **아직 코드화되지 않은 제안일 뿐**이다. 따라서 `A-L-37`의 `congestion_level`은 지금도 실제로는 거의 무의미한 좁은 범위 안에서 상/하가 자주 뒤바뀔 수 있는 상태다 — 이 매장의 혼잡도 값은 신뢰도가 낮다고 보고 해석해야 한다.

---

## 3. 코드 위치 요약

| 항목 | 파일 | 함수/상수 |
|---|---|---|
| schedule_status 판정 | `db/src/ami_db/status.py` | `determine_schedule_status()` |
| power_status 판정 | `db/src/ami_db/status.py` | `determine_power_status()` |
| final_status 매트릭스 | `db/src/ami_db/status.py` | `FINAL_STATUS_MATRIX`, `determine_final_status()` |
| congestion_level 분위수 계산 | `db/src/ami_db/status.py` | `compute_utilization_quartiles()` |
| congestion_level 판정 | `db/src/ami_db/status.py` | `determine_congestion_level()` |
| 배치 실행(전체 21개 매장 일괄 계산) | `db/scripts/09_compute_operating_status.py` | `compute_status_for_store()`, `main()` |
| API 응답 스키마 | `db/app/schemas.py` | `DayStatusRow`, `CurrentStatusSchema` 등 |
| API 응답 필드 설명 | `db/docs/API_REFERENCE.md` | (자동 생성) |
| 테이블 정의/CHECK 제약 | `db/sql/schema.sql`, `db/docs/SCHEMA.md` | `store_operating_status` |
